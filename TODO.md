# TODO

Free-form backlog. Add items any time; this session won't touch the file unless you ask.

## Conventions
- `[ ]` open, `[x]` done, `[~]` in progress, `[?]` needs more thought / blocked
- Date stamp in `YYYY-MM-DD` when useful
- Group by topic loosely; order within a section is priority-free unless marked
- Quote short context so the line makes sense in isolation ("when X happens, Y should Z")

---

## Viewer / UI


## Data — artifacts


## Data — concepts


## Scenarios


## Tooling / build


## Docs / README


## Deferred — revisit near 1.0

